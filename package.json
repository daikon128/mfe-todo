{
  "name": "mfe-todo",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "serve": "npm-run-all -p \"serve:*\"",
    "serve:app-shell": "npm --prefix ./app-shell run serve",
    "serve:gadget": "npm --prefix ./gadget run serve",
    "serve:todo-list": "npm --prefix ./todo-list run serve",
    "serve:todo-list-api": "npm --prefix ./todo-list-api run serve",
    "install-all": "npm-run-all -p \"install:*\"",
    "install:app-shell": "npm --prefix ./app-shell install",
    "install:gadget": "npm --prefix ./gadget install",
    "install:todo-list": "npm --prefix ./todo-list install",
    "install:todo-list-api": "npm --prefix ./todo-list-api install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
